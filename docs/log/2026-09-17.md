# 2026-09-17 · 킥오프: 세션 방식으로 재기획

## 배경
- 전작 `tweet-sweep`(공식 API)은 3만 건 기준 $150~300 → 사용자 결정으로 무료 세션 방식 Chrome 확장으로 재시작. 전작 폴더는 보류·보존

## 한 일
- 저장소 생성, 문서 골격. 헌장 v2.0(P0 위험 고지 추가), ADR-0001~0004, SRS v0.1(미정 Q1~Q4)
- 조사: X 웹 클라이언트 삭제 메커니즘(Cyd·국내외 확장 사례), Chrome MV3 제약, 확장 프레임워크(WXT·CRXJS·Plasmo)

## 알게 된 것
- Cyd: DeleteTweet 등 GraphQL 재전송, 식별자는 세션 트래픽에서 수집(회전 대응), 429 본문 "Rate limit exceeded"+헤더, 트랜잭션 헤더 생략
- 국내 확장(트청 도우미)은 UI 클릭 방식, Tweet-Removal-Tool은 GraphQL 600ms 간격(공격적)
- MV3: 서비스 워커 유휴 30s 종료, 핸들러 5분 상한, alarms ≥30s → 대시보드 페이지 오케스트레이터(ADR-0004)

## 막힌 것
- cubi.so는 SPA라 자동 수집으로 내용 확인 불가. 큐비 가격은 사용자 진술 기준
- Chrome 버전 확인용 PowerShell 명령이 bash 인용 문제로 실패 → 폴더 목록으로 대체

## 다음
- Q1~Q4(스택·실행기·속도·공개) 확정 → ADR-0005·0006, plan, tasks → GitHub 생성·푸시

## M1 스파이크 A 완료 (같은 날)
- Node 24.19 설치 → WXT 0.21 + React 19 + TS 5.9 + Vitest 4 골격. `srcDir: src`, 자동 임포트 끔, eslint로 `core` 브라우저 무의존 강제
- 구현: 모델·프로토콜 → 아카이브 파서(part 병합·구형 접두어·시간대 정규화·계정 email 폐기) → IndexedDB(idb, 7 스토어) → zip 입력(zip.js) → 가져오기 파이프라인 → 대시보드(고지·가져오기·분석·CSV) → 페이싱 산식
- 테스트 30개(파서·DB·페이싱·UI 스모크). 빌드 378 kB
- 결정(작음): zip은 fflate 대신 zip.js — ZIP64·임의 접근으로 수 GB 아카이브에서 두 파일만 추출. 워커는 CSP 때문에 끔
- 사용자 요구 추가: FR-11a 실시간 삭제 피드 + `deleted N of M tweets` 카운터(M3 T25)
- 막힌 것: Bash 대용량 heredoc 절단(재발) → Write 도구로 분할 · prettier가 fixture `.js`를 JS로 재포맷해 JSON 깨짐 → fixtures를 prettier/eslint 제외 · `.gitignore`의 `archive/`가 `src/core/archive`까지 무시 → 루트 앵커(`/archive/`)로 수정
- 다음: 사용자가 실제 아카이브로 analyze 실행 → 건수 확정 → M2(작업 탭·콘텐츠 스크립트·ui-click 1건·헤더 실측)

## M2 비파괴 부분 (같은 날)
- 실제 아카이브 검증: @gujik_man 28,144건 → 대상(원글+답글) 26,780, 리포스트 1,364. 파서·저장·분석 정상
- 구현: rate-limit 헤더 파싱 + x.com URL 유틸 → ui-click 실행기(페이지 판정·caret→메뉴→확인·twid) → 작업 탭 제어·콘텐츠 스크립트·webRequest 예산 관측 → 대시보드 삭제 테스트 화면. 매니페스트에 webRequest 추가
- 테스트 42개(파서·DB·페이싱·rate-limit·xurl·ui-click·UI). 빌드 386 kB
- 병렬 세션 충돌: 다른 세션이 같은 M2를 다른 설계로 작업해 `src/messaging/rpc.ts`·`src/core/rate-headers.ts`(둘 다 미추적·미참조, 존재하지 않는 타입 임포트로 빌드 파손)를 남김. 삭제 대신 스크래치패드 `parallel-session-orphans/`로 보존. 내 커밋 이력엔 외부 커밋 없음 확인. rate-headers의 `shouldWaitForReset`·`toBudget` 아이디어는 M3 스케줄러(T23)에서 참고 가능
- 라이브 검증 대기(T14): 사용자가 테스트 글 1건 + 삭제 승인 → 실제 삭제 경로·헤더 실측 → 프리셋 표 확정

## M2 라이브 검증 성공 (T14)
- 실계정 테스트 트윗 1건 UI 클릭으로 삭제 성공
- 디버깅 경로(사용자 진단 제보로 좁힘): No tab(작업 탭 소멸) → 전용 창+생존검사 · page unknown(SPA 렌더 지연) → 판정까지 폴링 · caret 못 찾음 → 대상 글을 첫 article로 · menuitem에 삭제 없음(`재게시 retweetConfirm | 인용하세요`) → caret 대체 셀렉터(aria-haspopup)가 재게시 버튼을 집던 버그. caret은 article 밖 헤더에 있어 문서 전체에서 잡도록 수정
- 교훈: 넓은 셀렉터(aria-haspopup)는 인접 버튼을 오집음. 실패 진단(DOM 수치+메뉴 라벨 덤프)이 원인 규명을 결정적으로 도움
- 다음 M3: 필터·페이싱·회로차단·스케줄러(순수 코어) → 실행 화면+실시간 피드/카운터

## M3 안전 엔진 코어 (순수, 같은 날)
- filters(6조건 조합) · breaker(429 사다리·잠금·연속오류) · pacing 런타임(워밍업·로그노멀 지터·긴 휴식·활동시간·예산 감시) · scheduler(결정 함수). 전부 순수 함수, 시각·난수 주입 → 결정적 테스트
- 테스트 총 76개. 하드 제약을 속성 테스트로 못박음(간격 ≥10s 등)
- 설계 이유: 안전 로직을 UI·브라우저와 분리(하네스). run 루프는 이 결정 함수를 호출만 → 강제 종료·재개가 단순
- 다음(T23b~T29): 대시보드 run 루프 + jobs/jobItems persistence + 실시간 피드/카운터(FR-11a) + 릴리스. 라이브 대량 실행은 워밍업(첫날 ≤50)부터, 사용자 시작

## M3 실행 경로 완성 (같은 날)
- persistence(createJob/nextPending/markItem/재개) + runner(코어 결정 구동) + 계획 화면(필터) + 실행 화면(실시간 피드·`deleted N of M` 카운터·진행바·일시정지/중지)
- 실측: 사용자 삭제 시 X 웹 클라이언트 예산 한도=200/잔여=198/리셋=106s → 적응형 페이싱이 brisk를 10s 하한으로 운용(26,780건 ≈ 5~6일). 백그라운드 webRequest가 실측 예산·429를 실시간 주입
- 테스트 82개, 빌드 401 kB. 라이브 대량 실행은 사용자가 시작(첫날 워밍업 ≤50, 활동시간 09~23, 429 시 자동 감속/정지)
- 다음: 사용자 소량 라이브 검증 → 감사 로그 CSV 내보내기 → M4(graphql 예비 실행기·확장 액션)
